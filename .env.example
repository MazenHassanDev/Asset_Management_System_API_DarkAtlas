# Django
SECRET_KEY=
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost

# Database
DB_NAME=
DB_USER=
DB_PASSWORD=
DB_HOST=
DB_PORT=

# Redis
REDIS_URL=

# Caching TTL - seconds (0 disbales it):
ASSET_CACHE_TTL=300

# Rate limiting (DRF throttle rates; format: "<num>/<period>", e.g. 1000/hour)
THROTTLE_ORG_RATE=1000/hour
THROTTLE_IMPORT_RATE=60/hour
